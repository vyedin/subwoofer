version: 1
frontend:
  phases:
    pre-build:
      commands: []
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
