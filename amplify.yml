version: 1
frontend:
  phases:
    pre-build:
      commands:
        - cd subwoofer-client
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths: []
