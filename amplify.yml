version: 1
frontend:
  phases:
    pre-build:
      commands:
        - cd subwoofer-client
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /subwoofer-client/build
    files:
      - '**/*'
  cache:
    paths: []
